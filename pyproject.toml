[project]
name = "prometheus-clinet-toolkit"
version = "0.1.0"
description = "A Simple Version of Async Prometheus Client"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.12.13",
]
